version: "3"
services:
  elasticsearch:
    image: elasticsearch:7.6.2
    environment:
      discovery.type: single-node
  kibana:
    image: kibana:7.6.2
    depends_on:
      - elasticsearch
    environment:
      elasticsearch.hosts: http://elasticsearch:9200
    ports:
      - "5601:5601"
